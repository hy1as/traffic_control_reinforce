# 강화학습 기반 교통 신호등 제어 프로젝트

## PPT 공유 링크
- PPTX
https://docs.google.com/presentation/d/1lXNSdVCZAhQrmAfthWR8Dkq5wLuDiP7I/edit?usp=drive_link&ouid=108528076737133136216&rtpof=true&sd=true

- PDF (권장)
https://drive.google.com/file/d/1adlHtxyN0nX5WEVgo9BC5L-TBWVdZY-H/view?usp=drive_link
## ❗️ 모델은 프로젝트 내에 포함 되어있습니다. 

/models/optimized

## 실행 방법

### 1. 패키지 설치

```bash
pip install -r requirements.txt
```

### 2. 데모 실행 (시각화 시연)

#### 시연 실행

**모든 시나리오 시각화 시연 (권장):**
```bash
# 모든 시나리오를 순차적으로 시각화하며 테스트 (고정 신호와 비교, 기본값)
python traffic_demo.py --scenario all --agent-type dqn --speed 0.3
```

**단일 시나리오 시각화 시연:**
```bash
# 평시 교통량 시나리오 (고정 신호와 비교, 기본값)
python traffic_demo.py --scenario normal --agent-type dqn --speed 0.3

# 출근 시간 혼잡 상황 비교
python traffic_demo.py --scenario morning_rush --agent-type ddqn --speed 0.5

# 극심한 혼잡 상황 (빠른 속도)
python traffic_demo.py --scenario congestion --agent-type ddqn --speed 0.2 --steps 300

# 비교 없이 단일 모델만 표시하려면
python traffic_demo.py --scenario normal --no-compare --agent-type dqn --speed 0.3
```

**고정 신호와 성능 비교 (기본값, 자동으로 비교 모드 활성화):**
```bash
# 평시 교통량에서 고정 신호와 비교 (--compare 옵션 불필요, 기본값)
python traffic_demo.py --scenario normal --agent-type dqn

# 출근 시간 혼잡 상황 비교
python traffic_demo.py --scenario morning_rush --agent-type ddqn --speed 0.3

# 고정 신호 주기 변경하여 비교
python traffic_demo.py --scenario normal --baseline-cycle 20 --agent-type dqn
```

**모든 시나리오 빠른 테스트:**
```bash
# 시각화 없이 빠르게 모든 시나리오 테스트
python traffic_demo.py --scenario all --no-visualize --agent-type dqn

# Double DQN으로 모든 시나리오 테스트
python traffic_demo.py --scenario all --no-visualize --agent-type ddqn
```

#### 데모 옵션 설명

**주요 옵션:**
- `--scenario`: 시나리오 선택 (필수)
  - `normal`: 균등한 교통량
  - `morning_rush`: 북/남 집중 (출근 시간)
  - `evening_rush`: 동/서 집중 (퇴근 시간)
  - `congestion`: 극심한 혼잡
  - `night`: 한산한 시간
  - `all`: 모든 시나리오 자동 테스트

- `--agent-type`: 알고리즘 선택
  - `dqn`: DQN 모델 사용
  - `ddqn`: Double DQN 모델 사용 (기본: dqn)

- `--speed`: 시뮬레이션 속도
  - 값이 작을수록 빠름 (예: 0.1 = 빠름, 1.0 = 보통, 2.0 = 느림)
  - 기본: 1.0초 (보기 좋은 속도)

- `--steps`: 최대 스텝 수
  - 지정하지 않으면 저장된 데이터 전체 실행

- `--no-visualize`: 시각화 없이 빠른 테스트
  - 모든 시나리오 테스트 시 유용
  - 결과만 요약해서 출력

- `--no-compare`: 비교 모드 비활성화 (기본값: 비교 모드 활성화)
  - 기본적으로 고정 신호와 성능 비교 모드가 활성화되어 있음
  - 강화학습 모델과 고정 신호를 나란히 비교
  - 실시간으로 성능 차이 확인 가능
  - 단일 모델만 표시하려면 이 옵션 사용

- `--baseline-cycle`: 고정 신호 주기 (초)
  - 기본값: 30초
  - 비교 모드에서만 사용

- `--model`: 모델 파일 경로
  - 지정하지 않으면 자동으로 찾음:
    - `./models/optimized/{agent_type}_{scenario}/agent_{scenario}_optimized.pt`

### 3. 전체 실험 실행

#### 마스터 스크립트 사용

```bash
# 전체 실험 (12-16시간, 최종 결과용)
python master_experiment.py --mode full

# 튜닝 스킵 (4-6시간, 기본 파라미터만)
python master_experiment.py --mode skip-tuning
```

**마스터 스크립트가 자동으로 수행**:
- ✅ 하이퍼파라미터 튜닝 (실험 A~D)
- ✅ 최적 파라미터 자동 선택
- ✅ 선택된 파라미터로 시나리오 비교
- ✅ 결과 자동 저장

#### 개별 스크립트 실행 (고급)

**1단계: 하이퍼파라미터 튜닝**
```bash
# 전체 튜닝 (1000 에피소드 × 3회 반복)
python hyperparameter_tuning.py
```

**2단계: 통합 워크플로우**
```bash
# 기본 파라미터로 시나리오 비교 (학습 곡선 그래프 포함)
python integrated_workflow.py

# 튜닝된 파라미터로 시나리오 비교 (학습 곡선 그래프 포함)
python integrated_workflow.py --use-tuned
```

---

## 📋 프로젝트 개요

DQN과 Double DQN을 활용한 스마트 교차로 신호등 제어 시스템

**핵심 특징**: 하이퍼파라미터 튜닝 → 최적 파라미터 자동 적용 → 시나리오 비교

## 🗂️ 파일 구조

```
project/
├── traffic_env.py                  # 교차로 환경 구현
├── dqn_agent.py                   # DQN/Double DQN 에이전트
│
├── hyperparameter_tuning.py       # 하이퍼파라미터 튜닝 실험
├── integrated_workflow.py         # 통합 워크플로우 (튜닝 → 시나리오)
├── master_experiment.py           # 마스터 스크립트 (전체 자동화)
│
├── generate_demo_data.py          # 시연용 가상 데이터 생성
├── traffic_demo.py                # 시연 스크립트
│
├── requirements.txt               # 패키지 의존성 목록
│
├── demo_data/                     # 생성된 가상 데이터
│   ├── normal_traffic_data.json
│   ├── morning_rush_traffic_data.json
│   ├── evening_rush_traffic_data.json
│   ├── congestion_traffic_data.json
│   └── night_traffic_data.json
│
├── models/                        # 학습된 모델
│   └── optimized/
│
├── results/                       # 실험 결과
│   ├── hyperparameter_tuning_results.json
│   ├── integrated_experiment_results.json
│   └── plots/
│
└── README.md
```

## 📊 주요 구성 요소

### 환경 (TrafficEnvironment)
- **State**: 7차원 벡터 (4개 차선 대기 차량 수 + 현재 신호 + 신호 지속 시간 + 시간대)
- **Action**: 2개 (신호 유지 / 신호 변경)
- **Reward**: -1.0 × 대기 차량 수 - 5.0 × 신호 변경 - 추가 페널티

### 시나리오
1. **normal**: 균등한 교통량 (0.2대/초)
2. **morning_rush**: 북/남 집중 (0.6대/초 vs 0.15대/초)
3. **evening_rush**: 동/서 집중 (0.15대/초 vs 0.6대/초)
4. **congestion**: 극심한 혼잡 (0.8대/초)
5. **night**: 한산한 시간 (0.05대/초)

### 알고리즘
- **DQN**: Experience Replay + Target Network
- **Double DQN**: DQN + Double Q-learning (과대 평가 방지)

## 📈 출력 결과

### 학습 중 출력
- Episode별 평균 Reward
- 평균 대기 시간
- 현재 탐색률 (ε)

### 저장 파일
- `./models/`: 학습된 모델 체크포인트
- `./results/experiment_results.json`: 전체 시나리오 비교 결과
- `./results/hyperparameter_tuning_results.json`: 하이퍼파라미터 튜닝 결과 ⭐
- `./results/plots/`: 학습 곡선 및 비교 그래프
  - `hyperparameter_lr.png`: Learning Rate 비교
  - `hyperparameter_gamma.png`: Discount Factor 비교
  - `hyperparameter_batch.png`: Batch Size 비교
  - `hyperparameter_buffer.png`: Buffer Size 비교

## 🎬 시연 화면 설명

시연 중 화면에는 다음 정보가 표시됩니다:

**일반 모드:**
```
======================================================================
  교통 신호등 제어 시뮬레이션 - Step   50 | 신호 지속:  15초
======================================================================

        [G] 북쪽: OOOOOO........ ( 6대)

        │
        │
서쪽: OOO........... ( 3대) ────┼──── 동쪽: OOOOOOOOOO..... (10대)
        [R]
        │
        │
        [G] 남쪽: OO............. ( 2대)

----------------------------------------------------------------------
  총 대기 차량:  21대  |  북/남:  8대  |  동/서: 13대
  선택된 행동: 신호 유지
  Q-value: 유지= -25.32  변경= -30.15
======================================================================
```

**비교 모드 (기본값, 자동 활성화):**
```
================================================================================================================================================
  교통 신호등 제어 성능 비교 - Step   50
================================================================================================================================================

강화학습 모델 (RL)                                                          │ 고정 신호 (Baseline)
------------------------------------------------------------------------------------------------------------------------------------------------
[G] 북: OOOOOO.. ( 6대)                                                    │ [R] 북: OOOOOOOO ( 8대)

서: OOO....... ( 3대) ──┼── 동: OOOOOOOO.. (10대)                        │ 서: OOOO...... ( 4대) ──┼── 동: OOOOOOOO.. (10대)
                                                                           │ 
[R]                                                                        │ [G]
                                                                           │ 
[G] 남: OO...... ( 2대)                                                    │ [R] 남: OOOO.... ( 4대)

------------------------------------------------------------------------------------------------------------------------------------------------
총 대기 차량:   21대                                                       │ 총 대기 차량:   26대
개선율:     19.2% 감소                                                     │ 
행동: 신호 유지                                                             │ 행동: 고정 주기
Q-value: 유지= -25.32 변경= -30.15                                         │ 
신호 지속:   15초                                                           │ 신호 지속:   25초
================================================================================================================================================
```

**표시 내용:**
- `[G]`: 초록불 (Green)
- `[R]`: 빨간불 (Red)
- `[Y]`: 노란불 (Yellow)
- `O`: 대기 중인 차량
- `.`: 빈 공간
- 숫자: 각 방향의 대기 차량 수

**통계 정보:**
- 총 대기 차량 수
- 방향별 대기 차량 수
- 선택된 행동 (신호 유지/변경)
- Q-value (각 행동의 예상 가치)

## 💡 데모 팁

1. **모든 시나리오 테스트**: `--scenario all --no-visualize`로 빠르게 모든 시나리오를 테스트하고 결과를 비교할 수 있습니다.

2. **시연 속도 조절**: `--speed` 값을 조절하여 시연 속도를 변경할 수 있습니다.
   - 빠른 시연: `--speed 0.3`
   - 보통 속도: `--speed 1.0` (기본값)
   - 느린 시연: `--speed 2.0`

3. **중단**: 시연 중 `Ctrl+C`로 언제든지 중단할 수 있습니다.

4. **데이터 재생성**: 다른 패턴을 보고 싶다면 `--seed` 값을 변경하여 데이터를 재생성하세요.

5. **모델 비교**: 같은 시나리오에서 `--agent-type`을 변경하여 DQN과 Double DQN의 차이를 비교할 수 있습니다.

6. **빠른 테스트**: 시각화 없이 빠르게 테스트하려면 `--no-visualize` 옵션을 사용하세요.

## 🔄 실험 워크플로우

```
┌─────────────────────────────────────────────────────────────┐
│                  Master Experiment (전체 자동화)              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 1단계: 하이퍼파라미터 튜닝                                     │
│ ─────────────────────────────────────────────────────────── │
│  • 실험 A: Learning Rate [0.0001, 0.001, 0.01]              │
│  • 실험 B: Discount Factor [0.90, 0.95, 0.99]               │
│  • 실험 C: Batch Size [32, 64, 128]                         │
│  • 실험 D: Buffer Size [5000, 10000, 20000]                 │
│                                                              │
│  각 설정당 3회 반복 (seeds: 42, 123, 456)                     │
│  1000 에피소드 학습                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 최적 하이퍼파라미터 자동 선택                                  │
│ ─────────────────────────────────────────────────────────── │
│  평균 Reward가 가장 높은 파라미터 선택                         │
│  예: lr=0.001, gamma=0.95, batch=64, buffer=10000           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2단계: 시나리오 비교 실험 (최적 파라미터 적용)                  │
│ ─────────────────────────────────────────────────────────── │
│  시나리오:                                                    │
│    • normal: 균등한 교통량                                    │
│    • morning_rush: 북/남 집중                                │
│    • evening_rush: 동/서 집중       
│    • night: 한산												│
│                                                              │
│  각 시나리오마다:                                             │
│    • DQN 학습 (2000 에피소드)                                │
│    • Double DQN 학습 (2000 에피소드)                         │
│    • Baseline 평가 (고정 30초)                               │
│    • 성능 비교 (대기시간, Reward)                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3단계: 결과 저장 및 시각화                                     │
│ ─────────────────────────────────────────────────────────── │
│  • JSON: 모든 실험 결과                                       │
│  • 그래프: 하이퍼파라미터 비교, 학습 곡선                       │
│  • 모델: 학습된 가중치 저장                                    │
│  • 요약 테이블: 성능 비교 및 개선율                            │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 평가 지표

1. **평균 대기 시간** (↓)
2. **Episode 누적 Reward** (↑)
3. **처리 차량 수** (↑)
4. **최대 대기 차량 수** (↓)

## 🔬 실험 설계

### 1. Baseline 비교
- 고정 주기 30초 신호등 vs DQN vs Double DQN

### 2. 시나리오 테스트
- 각 시나리오에서 알고리즘 성능 비교

### 3. 하이퍼파라미터 튜닝

각 실험마다 3개의 다른 시드(42, 123, 456)로 반복 실행하여 평균 및 표준편차 계산

| 실험 | 변수 | 테스트 값 | 고정 파라미터 |
|------|------|-----------|---------------|
| **실험 A** | Learning Rate | [0.0001, 0.001, 0.01] | 나머지 기본값 |
| **실험 B** | Discount Factor (γ) | [0.90, 0.95, 0.99] | 나머지 기본값 |
| **실험 C** | Batch Size | [32, 64, 128] | 나머지 기본값 |
| **실험 D** | Replay Buffer Size | [5000, 10000, 20000] | 나머지 기본값 |

**출력 결과**:
- 각 파라미터별 평균 Reward 및 표준편차
- 평균 대기 시간
- 평균 처리 차량 수
- 비교 그래프 (bar chart with error bars)

**실행 시간**: 약 12시간 (모든 실험 완료 시)